<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.jam.jam">

    <!-- Permission required -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

<!--    <uses-feature-->
<!--        android:name="android.hardware.bluetooth_le"-->
<!--        android:required="false" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Main -->
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Authentication -->
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.CreateAccountActivity" /> <!-- Online -->
        <activity android:name=".online.OnlineActivity" />
        <activity android:name=".online.OnlineChatActivity" />
        <activity android:name=".online.RequestsActivity" /> <!-- Offline -->
        <activity android:name=".offline.OfflineActivity" />
        <activity android:name=".offline.OfflineChatActivity" /> <!-- Profile -->
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".profile.UpdateProfileActivity" />
        <activity
            android:name=".profile.ViewProfileActivity"
            android:theme="@style/Theme.Dialog.NoActionBar" /> <!-- Settings -->
        <activity android:name=".settings.SettingsActivity" /> <!-- Help -->
        <activity android:name=".help.HelpActivity" /> <!-- About -->
        <activity android:name=".about.AboutActivity" /> <!-- optional (needed if default theme has no action bar) -->
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>